# 工程架构

## 模块边界

```text
browser input -> game controller -> game engine -> rules
                       |                |
                       |                +-> presentation port -> Canvas / HUD / effects / audio
                       +-> local / AI / online session

online session -> WebSocket client -> protocol validation -> authoritative server -> rules
```

- `rules.mjs` 是唯一规则实现，只处理局面、合法着法、成格、胜负和环面坐标；浏览器与服务端共用，不读取浏览器 API。
- `game-snapshot.mjs` 是规则状态与外部数据之间的版本化适配层。服务端通过 `toSnapshot` 输出 JSON DTO，客户端通过 `fromSnapshot` 恢复规则状态；规则字段变化必须提升 `rulesVersion` 并更新协议契约测试。
- `application/` 管用例和状态：`GameEngine` 管当前棋局、历史和对表现层发出的事件；`GameController` 是本地、AI、联机共用的命令入口；`SetupController` 只管开始页模式/棋盘/AI/房间选择；AI 与联机会话分别管理各自生命周期。
- `infrastructure/` 处理外部边界。`WebSocketClient` 只负责连接和消息收发；`protocol.mjs` 验证服务端消息与快照形状，验证失败不进入应用状态。
- `presentation/` 只把应用状态投影到 DOM；`board-view.mjs` 管 Canvas 绘制、几何命中和短暂的选择/悬停状态；`effects.mjs` 管效果数据，Canvas 决定如何画。
- `main.mjs` 是组合根，创建所有实例并注入依赖。运行代码不读写 `window.ZJ`，HTML 不再依赖经典脚本标签顺序。
- `server.mjs` 提供静态资源与 WebSocket 房间。房间局面由服务端持有，服务端验证命令后广播权威快照。

## 状态所有权

1. **棋局状态**：由规则引擎表示；在线对局只接受服务端经快照适配层验证和恢复的状态，本地与 AI 对局由客户端规则引擎推进。
2. **联机会话状态**：房间号、玩家身份、版本、待确认命令、重连和再赛请求由 `OnlineSession` 持有，不混入棋盘视图。
3. **界面状态**：选中格点、悬停位置、接缝观察原点和动画进度只由对应视图/特效系统持有，不发送到服务器。
4. **开始设置**：模式、棋盘大小和 AI 强度在开始页编辑；建立本地对局或成功加入房间后才用于创建棋局。联机时服务端创建的棋盘快照为准。

## 联机命令流程

玩家输入先由 controller 检查状态和回合权限。等待加入、断线、等待服务器确认、终局或不是自己的回合都会被拒绝并反馈；联机落子不会先改本地权威棋局。客户端发送带 `version` 和唯一 `commandId` 的命令；服务端再次验证玩家身份、版本、回合、坐标与规则，接受后推进版本并向双方广播。客户端验证快照后再一次性应用状态；旧版本丢弃，重复命令返回当前快照而不重复执行。

模式选择时只显示所选模式相关控件：AI 模式显示强度，联机模式显示创建/加入选择；房间号输入只在选择加入后展开。进入对局后 HUD 显示模式、AI 强度（适用时）、房间号、身份与连接/回合状态。非己方回合点棋盘会报告当前轮次；同一拒绝提示应避免因连续误触反复轰炸玩家。

联机模块不解释规则语义：`OnlineSession` 只发送命令、跟踪版本/连接和接收消息；服务端房间层调用规则内核验证命令；快照 DTO 由 `game-snapshot.mjs` 统一生成和恢复。这样替换规则实现时，只需保持快照契约和 `GameEngine` 所需的规则端口，联机连接状态无需改动。

种子判定只看是否连接到己方线：没有接上己方线就消耗一枚种子；所有接触都允许，只有覆盖已有线段才非法。房间创建后 HUD 提供复制房间号操作，复制能力在输入层触发，房间信息仍由会话状态提供。

## 动画约束

- `scheduler.mjs` 是唯一调用 `requestAnimationFrame` 的模块；没有动画、鼠标提示或 AI 思考时停止帧循环。
- `effects.mjs` 只存放效果状态，不创建自己的动画循环；棋盘 renderer 按局部坐标绘制，跨接缝的线段必须保持最短单格路径。
- 每局重置时清空效果与选择状态。异步 AI 任务通过取消令牌隔离旧局结果，远端快照用同一 presentation 入口更新显示。
- 尊重 `prefers-reduced-motion`，效果系统可在实例创建时禁用动态效果。

## 交互和安全约束

- 服务端永远不信任客户端权限检查；联机命令必须由房间席位、版本和规则重新验证。
- WebSocket 消息有大小上限；服务器不接受非整数坐标、版本或格式错误的命令。浏览器端也校验快照上限和数据结构。
- 房间恢复凭据只放在当前浏览器会话存储中；不把服务端消息拼进 HTML，着法列表使用 `textContent` 构造。
- HUD 的等待/错误/终局信息使用可访问的状态区域，棋盘以外的选择表单不吞掉页面快捷键。

## 技术选择

当前使用原生 ES Modules、HTML、CSS 和 Canvas。对单页棋盘游戏，这足以维持轻量部署；解耦来自依赖边界和状态所有权，不来自 UI 框架。服务端和浏览器共用规则实现，避免规则副本漂移。

下一阶段若需要更严谨的网络消息/状态契约，可单独引入 TypeScript，并用 Vite 管理检查与构建；Canvas 引擎仍可保持独立。暂不引入 React/Vue，因为当前界面规模尚不足以抵消迁移成本。

## 改规则时的工作流

1. 先在 `test/rules.test.mjs` 为新规则写失败的纯规则测试，不从 UI 或 WebSocket 测试开始。
2. 更新 `rules.mjs` 后运行规则测试，再更新 `game-snapshot.test.mjs` 和协议契约测试；不得让服务端自行补字段。
3. 用真实 WebSocket 集成测试验证服务端仍只接受合法命令、广播完整快照；用 UI smoke 验证快照进入棋盘后仍能渲染。
4. 只有单元、契约、集成和 UI 测试全部通过，才调整动画或文案。遇到 bug 时先补能复现它的测试，再修实现并复盘所属边界。

## 验证

`npm test` 覆盖规则、AI、会话与命令、UI/Canvas smoke 和本机 WebSocket 服务端集成。界面测试从 `index.html` 检查资源入口，并实际导入模块；协议测试覆盖错误快照和错误消息；服务端测试覆盖非法房间号、错误回合、错误命令、去重、重连、广播和再赛。
