# 《L》规则规范

## 1. 下棋

轮到一方时，在一个格点上画一条“横一格 + 竖一格”的折线，两条线共用一个格点，正好组成一个直角。

折线可以朝四个方向之一；两条目标线段中任意一条已被占用，该步不合法。

## 2. 胜利条件

己方格子在横、竖或斜方向连成 4 个不同的格子时立即获胜；连续关系允许跨越环面接缝。若当前玩家没有合法着法且没有胜者，则和棋。

## 3. 种子、重叠与续线

- 每名玩家有 3 枚种子。
- 折角与所有已有线都不相接时，必须消耗 1 枚种子；种子用完后不能在新位置落子。
- 新线不能压在已有线段上，但可以在格点处与对方的线交叉。
- 折角的三个格点中只要有一个连着自己的线，就属于续线，不消耗种子。

## 4. 棋盘

- 棋盘由 `W × H` 个方格组成，当前尺寸为 13、15、17、19 或 21。
- 棋盘是环面：左边与右边相连，上边与下边相连。
- 坐标越界时按棋盘尺寸取模，因此没有真正的边角。
- 每个格点连接四条单位线段，全盘共有 `2 × W × H` 条线段。

## 5. 玩家与线段

- 黑方先手，白方后手。
- 每条线段只能归黑方、白方或为空。
- 双方可以共享格点，但不能占用同一条线段。

## 6. 成格

落子后检查受影响的格子。一个格子只有在四条边全部存在且全部属于同一方时，才归该方所有。

如果四条边混有双方线段，该格保持中立；格子一旦被占领，归属不会改变。

## 7. 状态转移顺序

```text
验证折角
→ 放置两条线段
→ 种子落子则消耗一枚种子并记录种子位置
→ 更新成格
→ 检查当前玩家四连
→ 有胜者则结束
→ 否则切换回合
→ 下一方无合法着法则和棋
```

## 8. 非规则表现层

悔棋、AI 思考延迟、动画、音效、接缝滑块和 HUD 展示均不属于规则内核。规则内核必须是纯逻辑、可在 Node.js 和浏览器中独立运行的模块。
